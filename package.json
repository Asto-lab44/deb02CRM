{
  "name": "hub-astorya",
  "version": "1.0.0",
  "private": true,
  "description": "Hub Astorya — ERP + CRM + Hotline",
  "scripts": {
    "build": "node build.js",
    "prebuild": "rm -rf dist"
  },
  "devDependencies": {
    "@babel/core": "^7.29.7",
    "@babel/preset-react": "^7.29.7"
  }
}
