{
  "name": "hub-astorya-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only build tooling for Hub Astorya (precompile JSX). NOT used by Vercel.",
  "scripts": {
    "build": "rm -rf ../dist && node build.js"
  },
  "devDependencies": {
    "@babel/core": "^7.29.7",
    "@babel/plugin-transform-block-scoping": "^7.29.7",
    "@babel/preset-react": "^7.29.7"
  }
}
