// Menu utilisateur — désactivé (l'utilisateur préfère le menu intégré
// au bas de la sidebar de chaque page, comme avant). On garde le fichier
// pour ne pas casser les <script> existants dans les .html.
(function () {
  "use strict";
  // intentionnellement vide
})();
